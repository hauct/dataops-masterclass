# Thư viện bổ sung cho Airflow image. Pin để reproducible.
# (Airflow core & providers đã có sẵn trong base image apache/airflow:3.0.6)
apache-airflow-providers-postgres==6.1.0
apache-airflow-providers-amazon==9.5.0
# FAB auth manager: cần để đăng nhập bằng username/password (admin/admin).
# Airflow 3 mặc định dùng SimpleAuthManager (không có form login), nên ta cài & bật FAB.
apache-airflow-providers-fab>=2,<3
great-expectations==1.3.4
# Không pin pandas ở đây: để Great Expectations tự kéo bản tương thích (GE 1.3.4 yêu cầu pandas<2.2).
# Base image apache/airflow:3.0.6 đã có sẵn pandas/numpy phù hợp.
psycopg2-binary==2.9.10
